<Page
    x:Class="AudioUWP.View.Demo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AudioUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=demo}">
    <Page.Resources>
        <DataTemplate x:Key="ComboBoxItemTemplate">
            <TextBlock Margin="0,0,10,0" Text="{Binding Path=DisplayName}" />
        </DataTemplate>
        <DataTemplate x:Key="ResultsListViewTemplate">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" MinWidth="100"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Height="40" Width="40" Margin="5" VerticalAlignment="Top">
                    <Image Source="{Binding Path=GlyphBitmapImage}"
                           Stretch="UniformToFill"/>
                </Border>
                <Border Grid.Column="1" Margin="5">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Name:" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Path=Name}" FontWeight="Bold" TextWrapping="WrapWholeWords"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Id:" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Path=Id}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:ConnectionId='4' x:Name="RootGrid" Margin="12,20,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,15,0">
                    <TextBlock Margin="0,0,0,2" Text="Choose a device selector:" />
                    <ComboBox x:ConnectionId='7' x:Name="selectorComboBox" 
                              Margin="0,0,0,5"
                              HorizontalAlignment="Left" 
                              ItemTemplate="{StaticResource ComboBoxItemTemplate}" />

                    <StackPanel Orientation="Horizontal">
                        <Button x:ConnectionId='9' x:Name="startWatcherButton"
                                Margin="0,0,5,10"
                                Command="{Binding Path=Click_Start}"
                               >
                            Start Watcher
                        </Button>

                        <Button x:ConnectionId='10' x:Name="stopWatcherButton"
                                Margin="0,0,5,10"
                                IsEnabled="False"
                                Command="{Binding Path=Click_Stop}">
                            Stop Watcher
                        </Button>
                    </StackPanel>

                    <ListView x:ConnectionId='8' x:Name="resultsListView" BorderBrush="AntiqueWhite" BorderThickness="1"
                                MinHeight="20" Margin="0,10,0,0"
                                ItemTemplate="{StaticResource ResultsListViewTemplate}" Height="298" Width="1056"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Status Block for providing messages to the user.  Use the
             NotifyUser() method to populate the message -->
            <Border x:ConnectionId='5' x:Name="ErrorBorder" Background="Red" Grid.Row="2"/>
            <TextBlock x:ConnectionId='6' x:Name="StatusBlock" Grid.Row="1" Margin="12, 10, 12, 10" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page>

